<section id="inicio" class="containerHero mt-5">
    <div class="name">
        <h1>
            <div><span id="fullStack">Full Stack</span></div>
            <div><span id="developer">Developer</span></div>
        </h1>
        <h2>Carlos Guijosa</h2>
    </div>
    <div class="presentation">
        <p>
            Hola!, soy <span class="text-(--clr-accent)">Ingeniero en Gestión y Desarrollo de Software</span> con base en Morelia, Michoacán, especializado en <span class="text-(--clr-accent)">desarrollo full-stack</span> con tecnologías de vanguardia y <span class="text-(--clr-light-text)">automatización inteligente</span> de procesos empresariales. Actualmente expandiendo fronteras en <span class="text-(--clr-accent)">blockchain development</span> con <span class="text-(--clr-light-text)">Solidity</span>, automatización con <span class="text-(--clr-light-text)">agentes de IA</span> y plataformas <span class="text-(--clr-light-text)">no-code</span>, e integración de <span class="text-(--clr-light-text)">APIs de modelos de lenguaje (LLMs)</span>. 
        </p>
    </div>
    <div class="buttonsHero">
        <a id="viewProjects" href="#projects" class="btn btn-primary">
            <span>Ver proyectos</span>
            <svg
                class="btn-arrow"
                width="18"
                height="18"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2.5"
                stroke-linecap="round"
                stroke-linejoin="round"
                aria-hidden="true"
            >
                <line x1="5" y1="12" x2="19" y2="12"></line>
                <polyline points="12 5 19 12 12 19"></polyline>
            </svg>
        </a>
        <a id="contactMe" href="#contact" class="btn btn-secondary">
            <span>Contactarme</span>
        </a>
    </div>
</section>

<style>
    .containerHero {
        display: flex;
        flex-direction: column;
        text-align: center;
        justify-content: center;
        align-items: center;
        height: 100dvh;
        gap: 20px;
        background-color: var(--clr-dark-space);
        color: var(--clr-text-secundary);
        padding-left: 1rem;
        padding-right: 1rem;
    }

    .name{
        display: flex;
        flex-direction: column;
        align-items: center;
        margin-top: 30px
    }

    .name #fullStack,
    .name #developer {
        font-size: 3.5rem;
        color: var(--clr-light-text);
        line-height: 1;
    }

    .name #developer {
        color: var(--clr-accent);
    }

    .name h2 {
        font-size: 2.5rem;
        color: var(--clr-light-text);
        margin-top: 0.5rem;
    }

    .presentation {
        width: 90%;
        max-width: 700px;
    }

    .presentation p {
        font-size: 1rem;
    }

    .buttonsHero {
        display: flex;
        padding: 1vh;
        gap: 1rem;
        flex-wrap: wrap;
        justify-content: center;
        margin-top: 0.5rem;
    }

    .btn {
        --btn-ease: cubic-bezier(0.16, 1, 0.3, 1);
        position: relative;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        gap: 0.55rem;
        padding: 0.85rem 1.7rem;
        border-radius: 10px;
        font-weight: 600;
        font-size: 1rem;
        letter-spacing: 0.01em;
        border: 1px solid var(--clr-accent);
        overflow: hidden;
        isolation: isolate;
        transition:
            transform 0.4s var(--btn-ease),
            box-shadow 0.4s var(--btn-ease),
            background-color 0.3s ease,
            color 0.3s ease,
            border-color 0.3s ease;
    }

    .btn:hover {
        transform: translateY(-3px);
    }

    .btn:active {
        transform: translateY(-1px) scale(0.98);
    }

    /* Primario: relleno teal con glow + barrido de brillo */
    .btn-primary {
        background-color: var(--clr-accent);
        color: var(--clr-dark-space);
        box-shadow: 0 4px 16px -3px rgba(118, 171, 174, 0.45);
    }

    .btn-primary:hover {
        background-color: #8ec3c6;
        box-shadow: 0 12px 30px -6px rgba(118, 171, 174, 0.65);
    }

    .btn-primary::before {
        content: "";
        position: absolute;
        inset: 0;
        z-index: -1;
        background: linear-gradient(
            115deg,
            transparent 25%,
            rgba(255, 255, 255, 0.55) 50%,
            transparent 75%
        );
        transform: translateX(-130%);
        transition: transform 0.75s var(--btn-ease);
    }

    .btn-primary:hover::before {
        transform: translateX(130%);
    }

    .btn-arrow {
        transition: transform 0.4s var(--btn-ease);
    }

    .btn-primary:hover .btn-arrow {
        transform: translateX(4px);
    }

    /* Secundario: fantasma que se llena al pasar */
    .btn-secondary {
        background-color: transparent;
        color: var(--clr-light-text);
        border-color: var(--clr-accent-transparent);
    }

    .btn-secondary:hover {
        background-color: var(--clr-accent-transparent);
        border-color: var(--clr-accent);
        box-shadow: 0 10px 26px -8px rgba(118, 171, 174, 0.5);
    }

    @media (min-width: 768px) {
        .name #fullStack,
        .name #developer {
            font-size: 6rem;
        }

        .name h2 {
            font-size: 3rem;
        }

        .presentation {
            width: 50%;
        }

        .presentation p {
            font-size: 1.2rem;
        }
    }
</style>
